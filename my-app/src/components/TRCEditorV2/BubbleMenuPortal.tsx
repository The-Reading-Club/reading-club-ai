// import React from "react";
// import ReactDOM from "react-dom";

// const BubbleMenuPortal = ({ children }) => {
//   const portalRoot = document.getElementById("bubble-menu-root");
//   return portalRoot ? ReactDOM.createPortal(children, portalRoot) : null;
// };

// export default BubbleMenuPortal;
